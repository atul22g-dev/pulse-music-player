/**
 * PULSE config — player appearance options.
 *
 * Kept out of PlayerContext.jsx so that file exports only components and
 * hooks (react-refresh Fast Refresh boundary).
 */

/** Available accent colors for the player UI. */
export const ACCENTS = ["purple", "blue", "pink", "green", "orange"];

/** Available color themes for the player UI. */
export const THEMES = ["dark", "oled", "light"];
